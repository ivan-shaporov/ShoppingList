{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Deploy Bicep",
            "type": "shell",
            "command": "az deployment group create --resource-group ${input:resourceGroup} --template-file main.bicep --name deploy --parameters storageAccountName=${input:storageAccountName}",
            "problemMatcher": []
        }
    ],
    "inputs": [
        {
            "id": "resourceGroup",
            "type": "promptString",
            "description": "Enter the Resource Group name",
            "default": "checklist"
        },
        {
            "id": "storageAccountName",
            "type": "promptString",
            "description": "Enter the Storage Account Name (globally unique)",
            "default": "checklist"
        }
    ]
}